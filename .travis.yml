language: php

matrix:
  include:
    - php: 7.2
      env: dependencies=lowest
    - php: 7.2
      env: dependencies=highest
    - php: 7.3
      env: dependencies=lowest
    - php: 7.3
      env: dependencies=highest
    - php: 7.4snapshot
      env: dependencies=lowest
    - php: 7.4snapshot
      env: dependencies=highest
  allow_failures:
    - php: 7.4snapshot

before_script:
  - echo 'Europe/Zurich' | sudo tee /etc/timezone
  - echo 'date.timezone = "Europe/Zurich"' >> ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/travis.ini
  - composer self-update -q
  - if [ -z "$dependencies" ]; then composer install; fi;
  - if [ "$dependencies" = "lowest" ]; then composer update --prefer-lowest -n; fi;
  - if [ "$dependencies" = "highest" ]; then composer update -n; fi;

script:
  - phpdbg -qrr vendor/bin/phpunit --coverage-text --verbose
  - phpdbg -qrr vendor/bin/infection --threads=$(nproc) --min-msi=89 --verbose
  - vendor/bin/phpstan analyse
  - vendor/bin/phploc src --verbose
  - vendor/bin/php-cs-fixer fix --dry-run --stop-on-violation
  - if [ -z "$dependencies" ]; then vendor/bin/phpinsights analyse --no-interaction --min-quality=81; fi;
  - if [ "$dependencies" = "highest" ]; then vendor/bin/phpinsights analyse --no-interaction --min-quality=81; fi;
  - if [ "$dependencies" = "lowest" ]; then vendor/bin/phpinsights analyse --no-interaction --min-quality=81 --disable-security-check; fi;
